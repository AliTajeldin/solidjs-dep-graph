{
  "name": "solidjs-dep-graph",
  "version": "0.0.1",
  "description": "dependency graphs in solidjs",
  "license": "APACHE",
  "scripts": {
    "start": "xxvite",
    "dev": "xxvite",
    "build": "pnpm --filter ./lib build",
    "serve": "xxvite preview",
    "clobber": "rm -rf node_modules dist && pnpm run -r clobber"
  },
  "engines": {
    "node": ">=14"
  }
}
