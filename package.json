{
  "name": "solidjs-dep-graph",
  "version": "0.0.1",
  "description": "dependency graphs in solidjs",
  "license": "APACHE",
  "scripts": {
    "dev": "cd ./demo && pnpm dev",
    "build": "pnpm -r build",
    "preview": "cd ./demo && pnpm preview",
    "clobber": "rm -rf node_modules dist && pnpm run -r clobber"
  },
  "engines": {
    "node": ">=14"
  }
}
